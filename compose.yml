# High availability DNS stack (Docker Compose 2.24+ `include`). From repo root:
#   docker compose --env-file dns-setup/.env.vm1 up -d
# Or run dns-setup/deploy.sh (multi-file compose; works on older Compose).
name: pihole

include:
  - path: dnscrypt-proxy/compose.yml
  - path: pihole/compose.yml
  - path: keepalived/compose.yml
