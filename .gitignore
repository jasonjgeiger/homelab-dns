.specstory/
# dns-setup — optional local overrides (legacy)
dns-setup/.env

# Per-service secrets (copy from each `.env.example`)
macvlan/.env
dnscrypt-proxy/.env
pihole/.env
keepalived/.env

# Rendered from keepalived/keepalived.conf.template
keepalived/keepalived.conf

# nebula-sync — local credentials
nebula-sync/.env
